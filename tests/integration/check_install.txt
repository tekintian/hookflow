! exec hookflow check-install
exec git init
exec git config user.email "you@example.com"
exec git config user.name "Your Name"
exec git add -A
! exec hookflow check-install
exec hookflow install
exec hookflow check-install

-- hookflow.yml --
pre-commit:
  jobs:
    - run: echo hello, test
