exec git init
exec hookflow install
exec git config user.email "you@example.com"
exec git config user.name "Your Name"
exec git add file.txt

! exec hookflow run pre-commit --fail-on-changes
stdout '│  Error: files were modified by a hook, and fail_on_changes is enabled'

! exec hookflow run hook-setting
stdout '│  Error: files were modified by a hook, and fail_on_changes is enabled'

exec hookflow run hook-setting --fail-on-changes=false

-- hookflow.yml --
pre-commit:
  commands:
    edit_file:
      run: echo newline >> file.txt
      stage_fixed: true

hook-setting:
  fail_on_changes: true
  jobs:
    - name: edit_file
      run: echo newline >> file.txt
      stage_fixed: true

-- file.txt --
1
