exec git init
exec git config user.email "you@example.com"
exec git config user.name "Your Name"
exec git add -A
exec git commit -m 'commit 1'
exec hookflow install

exec git checkout -b merge-me
exec touch file.A
exec git add -A
exec git commit -m 'commit A'

exec hookflow run pre-commit --force
stdout 'skip-merge-commit'

exec git checkout -
exec touch file.B
exec git add -A
exec git commit -m 'commit B'

exec git merge merge-me

exec hookflow run pre-commit --force
! stdout 'skip-merge-commit'

-- hookflow.yml --
output:
  - execution_out

pre-commit:
  commands:
    skip-merge-commit:
      skip:
        - merge-commit
      run: echo 'skip-merge-commit'
