exec git init
exec git config user.email "you@example.com"
exec git config user.name "Your Name"
exec git add -A
exec hookflow install
exec git commit -m 'test'
stderr '\s*Args: hookflow.yml\s*'

-- hookflow.yml --
output:
  - execution_out

pre-commit:
  jobs:
    - script: file.sh
      runner: sh
      args: "{files}"
      files: echo hookflow.yml

-- .hookflow/pre-commit/file.sh --
#!/usr/bin/env sh

echo "Args: $@"
