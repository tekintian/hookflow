exec git init
exec hookflow install
exec git add -A

exec hookflow run echo
stdout 'a-file\.js'

exec hookflow run echo --all-files
stdout 'a-file\.js b_file\.go c,file\.rb'

exec hookflow run echo --file a-file.js --file ghost.file
stdout 'a-file\.js ghost\.file'

-- hookflow.yml --
output:
  - execution_out

echo:
  commands:
    echo:
      files: echo a-file.js
      run: echo "{files}"

-- a-file.js --
a-file.js

-- b_file.go --
b_file.go

-- c,file.rb --
c,file.rb
