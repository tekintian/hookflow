exec git init
exec hookflow install pre-commit post-commit
! stderr .

exists hookflow.yml
exists .git/hooks/pre-commit
exists .git/hooks/post-commit
! exists .git/hooks/pre-push

-- hookflow.yml --
pre-commit:
  jobs:
    - run: echo

post-commit:
  jobs:
    - run: echo

pre-push:
  jobs:
    - run: echo

