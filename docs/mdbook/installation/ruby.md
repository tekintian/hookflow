## Ruby

```ruby
# Gemfile

group :development do
  gem "hookflow", require: false
end
```

Or globally

```bash
gem install hookflow
```

**Troubleshooting**

If you see the error `hookflow: command not found` you need to check your $PATH. Also try to restart your terminal.
