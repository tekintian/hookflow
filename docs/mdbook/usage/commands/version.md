## `hookflow version`

`hookflow version` prints the current binary version. Print the commit hash with `hookflow version --full`

**Example**

```bash
$ hookflow version --full

1.1.3 bb099d13c24114d2859815d9d23671a32932ffe2
```

