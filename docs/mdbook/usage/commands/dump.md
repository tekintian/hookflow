## `hookflow dump`

Prints the whole configuration after merging all secondary configs.

This is the actual config hookflow uses, it can be build from the main config (`hookflow.yml`), remotes, extends, and `hookflow-local.yml` overrides.

