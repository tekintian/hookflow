## `hookflow self-update`

Updates the binary with the latest hookflow release on Github.

This command is available only if you install hookflow from sources or download the binary from the Github Releases. For other ways use package-specific commands to update hookflow.
