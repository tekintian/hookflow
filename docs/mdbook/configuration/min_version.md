## `min_version`

If you want to specify a minimum version for hookflow binary (e.g. if you need some features older versions don't have) you can set this option.

**Example**

```yml
# hookflow.yml

min_version: 1.1.3
```
