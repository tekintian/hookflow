## `source_dir_local`

**Default: `.hookflow-local/`**

Change a directory for *local* script files (not stored in VCS).

This option is useful if you have a `hookflow-local.yml` config file and want to reference different scripts there.

